NEXT_PUBLIC_API_URL=http://localhost:4000
# The GitHub App's URL slug (not its numeric APP_ID) — used to build the
# https://github.com/apps/<slug>/installations/new install link on the Onboarding screen.
NEXT_PUBLIC_GITHUB_APP_SLUG=replace-with-github-app-slug
