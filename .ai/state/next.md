# Next Tasks
> Queued — not yet started.

1. Auth screens — register (2-step w/ OTP), login, AuthProvider rehydration (frontend Step 2)
2. Install flow (frontend Step 3) — can now call the completed `/github/*` and `/repos/*` APIs
3. Billing module (backend Step 6) — Stripe client, plans/checkout/portal, webhook + idempotency
4. Review dashboard screens (frontend) — can now call the completed `/reviews/*` APIs
