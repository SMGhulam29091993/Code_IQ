# Next Tasks
> Queued — not yet started.

1. Auth screens — register (2-step w/ OTP), login, AuthProvider rehydration (frontend Step 2)
2. Repos module — list/activate/deactivate/config, effective-config resolution via
   `.codeiq.yml` (backend Step 4). Needs a decision on how `Repo` rows first get created/synced
   from GitHub — see the open question in `knowledge/domains/github-app.md`.
3. Install flow (frontend Step 3) — can now call the completed `/github/*` API
